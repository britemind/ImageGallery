@model IEnumerable<ImageGallery.Models.Gallery>
@{ViewBag.Title = "Index2";}
<h2>Index2</h2>
<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th></th>
    </tr>
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.id.ToString() }) |
                @Html.ActionLink("Details", "Details", new { id = item.id.ToString() }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.id.ToString() })
                @Html.ActionLink("Add File", "AddFile", new { id = item.id.ToString() })
                @Html.ActionLink("Thumbnail View", "ThumbnailView", new { id = item.id.ToString() })
            </td>
        </tr>
    }
</table>
